// import LoginUser from "../../src/application/usecase/Login";
// import LoginRepositoryDataBase from "../../src/infra/repository/LoginRepositoryDataBase";
// import { tokenVerify } from "../../src/middlewares/tokenVerify";

// jest.mock('../../src/middlewares/tokenVerify'); // Mock tokenVerify for testing

// test('Deve realizar o login', async () => {
//   const input = {
//     email: "brunapereira@studio.com.br",
//     password: "Bruna24"
//   };
//   const useCase = new LoginUser(new LoginRepositoryDataBase());
//   const output = await useCase.execute(input);

//   const mockRequest = jest.fn() as Request<any, any, any, any>; // Mock Request with Jest
//   mockRequest.headers = {};
//   mockRequest.headers.authorization = `Bearer ${output.token}`;

//   mockRequest.get = jest.fn((headerName: string) => {
//     if (headerName === 'authorization') {
//       return mockRequest.headers.authorization;
//     }
//     return ''; // Retorna string vazia para outros headers
//   });

//   const mockResponse = {};

//   await tokenVerify(mockRequest, mockResponse);

//   expect(output.user).toBeDefined();
//   expect(output.token).toBeDefined();
// });
